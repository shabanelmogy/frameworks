@{
    ViewData["Title"] = "Services";
}

<style>
.lesson-container { max-width: 900px; margin: 0 auto; padding-bottom: 3rem; }
.lesson-intro { margin-bottom: 2rem; color: #8b949e; font-size: 1.1rem; }
.example-card { background: var(--card-bg, #21262d); border: 1px solid var(--border, #30363d); border-radius: 8px; padding: 1.5rem; margin-bottom: 2rem; }
</style>

<div class="lesson-container">
  <h2>Services & HTTP</h2>
  <p class="lesson-intro">In ASP.NET Core MVC, you typically inject services into the Controller and pass models to the View, or use the <code>@@inject</code> directive in Razor.</p>

  <div class="example-card">
    <p style="color: #e6edf3;">This placeholder replicates the Services demo. To see the full implementation of fetching data, please refer to the Angular project or implement an MVC API controller and fetch it using JS `fetch()` on the client.</p>
  </div>
</div>
